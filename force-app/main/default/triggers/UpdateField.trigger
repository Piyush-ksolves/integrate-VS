//whenever new contact created the testfield automatically get the value
trigger UpdateField on Contact (before insert) {
	
    //UpdateFIeldHandler.updateContactRecord(Trigger.New);
    //TestUpdateContactRecord.newData();
    
        
}